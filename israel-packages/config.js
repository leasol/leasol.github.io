/*
 * Optional browser-only testing configuration.
 * Never commit a real API key here; production uses the server secret.
 */
window.ISRAEL_PACKAGES_CONFIG = {
  openaiApiKey: "YOUR_OPENAI_API_KEY"
};
